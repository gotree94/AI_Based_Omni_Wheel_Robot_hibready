## 2.2 ROS 준비

객체 추적 실습을 위해서는 ROS 통시늘 이용하여 객체 감지 결과를 로봇 제어 프로세스에 전달한다.

(1) ROS 통신 설정을 위해 Jupyter의 우측 메뉴에서 터미널을 실행한다.



(2) Jupyter Notebook에서 터미널이 실행된다.



(3) ROS Master 실행을 위해 터미널 창에 아래 명령어를 입력한다.

```
roscore
```


(4) 터미널 창에서 ROS Master가 정상적으로 실행됨을 볼 수 있다.
